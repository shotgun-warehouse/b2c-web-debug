import styled from '@emotion/styled';

const Wrapper = styled.div`
  max-width: 100rem;
  margin-left: auto;
  margin-right: auto;
  padding-left: 2rem;
  padding-right: 2rem;
`;

export default Wrapper;
